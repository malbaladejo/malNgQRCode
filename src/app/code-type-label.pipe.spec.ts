import { CodeTypeLabelPipe } from './code-type-label.pipe';

describe('CodeTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
